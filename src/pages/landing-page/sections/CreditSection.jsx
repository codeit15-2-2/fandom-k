import { DEFAULT_STYLES } from '@pages/landing-page/constants/stylesConstant';

const CreditSection = () => {
  return (
    <div className={DEFAULT_STYLES.SNAP_CONTAINER}>
      CreditSection
    </div>
  );
};

export default CreditSection;
