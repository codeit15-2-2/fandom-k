import { cn } from '@libs/cn';
import { SNAP_ITEM } from '@pages/landing-page/constants/layouts';

const CreditSection = () => {
  return <div className={cn(SNAP_ITEM, 'bg-black')}>CreditSection</div>;
};

export default CreditSection;
