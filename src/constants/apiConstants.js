const API_TIMEOUT = 5000;

const HEADERS = {
  JSON: {
    'Content-Type': 'application/json',
    Accept: 'application/json',
  },
  FORM: {
    'Content-Type': 'multipart/form-data',
  },
};

export { API_TIMEOUT, HEADERS };
